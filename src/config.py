"""Configuration helpers for the melanoma classifier project.

This module currently serves as a placeholder for environment-specific
constants such as dataset paths or API keys. It exists to centralise future
settings so that experiment scripts can import them from a single location.
"""

